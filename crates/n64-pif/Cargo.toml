[package]
name = "n64-pif"
description = "The PIF of the Nintendo 64, primarily used for communication with external devices"
version = "0.1.0"
edition = "2021"

[dependencies]
n64-common = { path = "../n64-common" }
strum = { version = "0.26.1", features = ["derive"] }
